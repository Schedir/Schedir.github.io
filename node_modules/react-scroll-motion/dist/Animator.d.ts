import { FC, ReactNode } from "react";
import { Animation } from "react-scroll-motion/dist/types";
interface AnimatorProps {
    children: ReactNode | ReactNode[];
    animation: Animation;
}
declare const Animator: FC<AnimatorProps>;
export default Animator;
